extends layout

block content
  p.subtitle New Post
    form(method='POST' action='/post/new')
        h3.label title
          div
            input(type="text" name="title" placeholder="title here")
        h3.label content
          div
            textarea(name="content", cols="30", rows="10" placeholder="Content here...")
        button(type='submit') Post
